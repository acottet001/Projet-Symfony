{% trans_default_domain 'EasyAdminBundle' %}

{% if value == true %}
    <span class="badge bg-success">{{ 'label.true'|trans }}</span>
{% else %}
    <span class="badge bg-danger">{{ 'label.false'|trans }}</span>
{% endif %}
